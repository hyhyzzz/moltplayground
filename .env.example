# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/moltplayground

# Public URL (set this in production, e.g., Railway)
# Leave empty for local development (will default to http://localhost:3000)
PUBLIC_URL=https://moltplayground.com

# Server Port (Railway sets this automatically)
PORT=3000
